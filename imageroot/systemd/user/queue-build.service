# queue-build.service

[Unit]
Description=Podman queue-build.service
Wants=erp-next.service
After=erp-next.service  redis-cache.service mariadb-app.service  configurator.service create-site.service frontend.service

[Service]
Environment=PODMAN_SYSTEMD_UNIT=%n
EnvironmentFile=%S/state/environment
Restart=always
TimeoutStopSec=70
ExecStartPre=/bin/rm -f %t/queue-build.pid %t/queue-build.ctr-id
ExecStart=/usr/bin/podman run --conmon-pidfile %t/queue-build.pid \
    --cidfile %t/queue-build.ctr-id --cgroups=no-conmon \
     --pod-id-file %t/erp-next.pod-id --replace -d --name queue-build \
    -v sites:/home/frappe/frappe-bench/sites \
    -v logs:/home/frappe/frappe-bench/logs \
    -v certbot:/home/frappe/frappe-bench/.certbot:Z \
    -v docker-builds:/home/frappe/frappe-bench/.docker-builds:Z \
    -v clones:/home/frappe/frappe-bench/.clones:Z \
    -v %S/state/.ssh/:/home/frappe/.ssh/:Z \
    ${PRESS_IMAGE} bench worker --queue build
ExecStop=/usr/bin/podman stop --ignore --cidfile %t/queue-build.ctr-id -t 10
ExecReload=/usr/bin/podman kill -s HUP queue-build
SyslogIdentifier=%u
ExecStopPost=/usr/bin/podman rm --ignore -f --cidfile %t/queue-build.ctr-id
PIDFile=%t/queue-build.pid
Type=forking

[Install]
WantedBy=default.target
